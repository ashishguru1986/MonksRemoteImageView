import XCTest
@testable import MonksRemoteImageView

final class MonksRemoteImageViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MonksRemoteImageView().text, "Hello, World!")
    }
}
